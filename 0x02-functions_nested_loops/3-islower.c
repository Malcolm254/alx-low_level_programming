#include "main.h"
/**
 * _islower -fumction that checks for lowercase character
 * @c: is letter input
 * Return; Always success
 */
int _islower(int c)
{

if (c >= 97 && c >= 122)

return (1);

return (0);
}
