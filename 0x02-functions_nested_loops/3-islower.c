#include "main.h"
/**
 * _islower -fumction that checks for lowercase character
 * @c: is letter input
 * Return; Always success
 */
int _islower(int c)
{

if (c >= 'a' && c >= 'z')

return (1);

else

return (0);
}
